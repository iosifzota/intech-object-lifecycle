#ifndef __TESTMOVE_H__
#define __TESTMOVE_H__

#include "Point2D.hpp"
#include <utility>

using namespace std;

void testMove()
{
    // Uncomment these lines. It should already work because you did the implementation.
    // Point2D pct1(3, 4);
    // Point2D pct2(pct1);
    // Point2D pct3(11, 12);
    // pct2 = pct3;
    
    // Uncomment and do the implmentation one line at a time.
    // Point2D pct4( move(pct1) );
    // pct4 = move(pct2);

    // At this point what should be the content of pct1?
    // How about pct2?
}
#endif // __TESTMOVE_H__